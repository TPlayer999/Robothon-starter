# TACTILE-DEX runtime dependencies.
# Tested on macOS arm64 (Apple M2) with Python 3.12.
mujoco>=3.1.3
numpy>=1.24,<2
imageio[ffmpeg]>=2.31
gymnasium>=0.29
stable-baselines3>=2.0
torch>=2.1            # CPU build on macOS; install via the CPU wheel index
tensorboard>=2.14
matplotlib>=3.7
pyyaml>=6.0
